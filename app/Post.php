<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Post extends Model
{
    /**
     * We can:
     *
     * set table name
     * change primary key filed
     * specify timestamps for  (true by default)
     */
    // protected $table = 'posts';
    // public $primaryKey = 'id';
    // public $timestamps = true;
}
